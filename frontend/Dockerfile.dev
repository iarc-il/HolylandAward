# Development Dockerfile for frontend with hot reload
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

EXPOSE 5173

# Start development server
# If node_modules exists (mounted from host), use it
# Otherwise, install dependencies in container
CMD sh -c "if [ ! -d node_modules ]; then npm install; fi && npm run dev -- --host 0.0.0.0"
